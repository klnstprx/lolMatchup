name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Install templ
        run: go install github.com/a-h/templ/cmd/templ@latest

      - name: Generate templates
        run: templ generate

      - name: Check formatting
        run: test -z "$(gofmt -s -l .)" || (echo "Files need gofmt:"; gofmt -s -l .; exit 1)

      - name: Vet
        run: go vet ./...

      - name: Build
        run: go build -o lolmatchup.bin

      - name: Test
        run: go test ./... -cover
